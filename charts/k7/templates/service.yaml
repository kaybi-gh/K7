apiVersion: v1
kind: Service
metadata:
  name: {{ include "k7.fullname" . }}
  labels:
    {{- include "k7.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "k7.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
