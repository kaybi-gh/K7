
<MudDialog>
    <DialogContent>
        @if (_loading)
        {
            <MudProgressLinear Indeterminate="true" Color="Color.Primary" />
        }
        else
        {
            <MudText Typo="Typo.body2" Color="Color.Secondary" Class="mb-3">
                Décochez les librairies auxquelles l'utilisateur ne doit pas avoir accès.
            </MudText>
            <MudStack Spacing="1">
                @foreach (var library in _libraries)
                {
                    var excluded = _excludedIds.Contains(library.Id);
                    <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
                        <MudText Typo="Typo.body2">@library.Title</MudText>
                        <MudSwitch T="bool" Value="@(!excluded)" ValueChanged="@(v => ToggleExclusion(library.Id, !v))"
                                   Color="Color.Primary" />
                    </MudStack>
                }
            </MudStack>
        }
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Annuler</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit">Enregistrer</MudButton>
    </DialogActions>
</MudDialog>
