@namespace K7.Clients.Shared.UI.Components

<button class="k7-btn k7-btn--@Variant focusable @(string.IsNullOrEmpty(Color) ? "" : $"k7-btn--{Color}") @(Size == "sm" ? "k7-btn--sm" : Size == "lg" ? "k7-btn--lg" : Size == "xl" ? "k7-btn--xl" : "") @Class"
        type="@Type"
        disabled="@Disabled"
        @onclick="OnClick"
        @attributes="AdditionalAttributes">
    @if (!string.IsNullOrEmpty(StartIcon))
    {
        <K7Icon Icon="@StartIcon" />
    }
    @ChildContent
    @if (!string.IsNullOrEmpty(EndIcon))
    {
        <K7Icon Icon="@EndIcon" />
    }
</button>
