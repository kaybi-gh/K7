@namespace K7.Clients.Shared.UI.Components
@using K7.Clients.Shared.Helpers

<div class="music-ambient-wash @(DominantColor is null ? "music-ambient-wash--neutral" : "")"
     style="@DominantColorCss.ToVariableStyle("--ambient-color", DominantColor)"
     aria-hidden="true">
</div>

@code {
    [Parameter]
    public string? DominantColor { get; set; }
}
