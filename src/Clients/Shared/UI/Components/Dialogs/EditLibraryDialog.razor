@inject IStringLocalizer<EditLibraryDialog> L

<MudDialog>
    <DialogContent>
        <MudStack Spacing="4">
            <MudTextField @bind-Value="Title" Label="@L["TitleLabel"]" Variant="Variant.Outlined"
                          Required="true" RequiredError="@L["TitleRequired"]" />

            <MudSelect T="string" @bind-Value="SelectedProvider" Label="@L["ProviderLabel"]" Variant="Variant.Outlined"
                       Required="true" RequiredError="@L["ProviderRequired"]">
                @foreach (var provider in AvailableProviders)
                {
                    <MudSelectItem T="string" Value="@provider.ProviderName">@provider.ProviderName</MudSelectItem>
                }
            </MudSelect>

            <MudNumericField T="int?" @bind-Value="MetadataRefreshIntervalDays"
                             Label="@L["MetadataRefreshLabel"]" Variant="Variant.Outlined"
                             Min="0" Max="365"
                             HelperText="@L["MetadataRefreshHelper"]" />
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">@L["Cancel"]</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="Submit"
                   Disabled="@(!CanSubmit)">
            @L["Save"]
        </MudButton>
    </DialogActions>
</MudDialog>
