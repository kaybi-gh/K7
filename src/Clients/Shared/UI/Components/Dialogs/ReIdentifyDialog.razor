@inject IStringLocalizer<ReIdentifyDialog> L
@inject IStringLocalizer<SharedResource> S

<MudDialog>
    <DialogContent>
        <MudStack Spacing="4">
            <MudTextField @bind-Value="_searchQuery" Label="@L["TitleLabel"]" Variant="Variant.Outlined" Adornment="Adornment.End" AdornmentIcon="@Icons.Material.Filled.Search" OnAdornmentClick="SearchAsync" OnKeyDown="HandleKeyDown" />
            
            <MudStack Row="true" Spacing="4">
                <MudNumericField @bind-Value="_searchYear" Label="@L["YearOptionalLabel"]" Variant="Variant.Outlined" Clearable="true" />
                <MudTextField @bind-Value="_searchProviderId" Label="@L["ProviderIdLabel"]" Variant="Variant.Outlined" Clearable="true" />
            </MudStack>
            
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="SearchAsync" Disabled="_isSearching">
                @if (_isSearching)
                {
                    <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
                    <MudText Class="ms-2">@L["Searching"]</MudText>
                }
                else
                {
                    <MudText>@L["SearchButton"]</MudText>
                }
            </MudButton>

            @if (_results != null)
            {
                if (!_results.Any())
                {
                    <MudText Color="Color.Warning">@L["NoResults"]</MudText>
                }
                else
                {
                    <MudList T="MetadataSearchResult" SelectionMode="SelectionMode.SingleSelection" @bind-SelectedValue="_selectedResult" Style="max-height: 400px; overflow-y: auto;">
                        @foreach (var result in _results)
                        {
                            <MudListItem Value="result">
                                <MudGrid>
                                    <MudItem xs="3">
                                        @if (!string.IsNullOrEmpty(result.PosterUrl))
                                        {
                                            <MudImage Src="@result.PosterUrl" ObjectFit="ObjectFit.Cover" Height="100" Width="70" Elevation="25" Class="rounded-lg" loading="lazy" />
                                        }
                                        else
                                        {
                                            <MudPaper Height="100px" Width="70px" Class="d-flex align-center justify-center rounded-lg">
                                                <MudIcon Icon="@Icons.Material.Filled.ImageNotSupported" />
                                            </MudPaper>
                                        }
                                    </MudItem>
                                    <MudItem xs="9">
                                        <MudStack Spacing="0">
                                            <MudText Typo="Typo.subtitle1"><b>@result.Title</b> @(result.Year.HasValue ? $"({result.Year})" : "")</MudText>
                                            <MudText Typo="Typo.caption" Color="Color.Secondary">@string.Format(L["ProviderAndId"], result.Provider, result.ExternalId)</MudText>
                                            <MudText Typo="Typo.body2" Style="overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 3; -webkit-box-orient: vertical;">@result.Overview</MudText>
                                        </MudStack>
                                    </MudItem>
                                </MudGrid>
                            </MudListItem>
                        }
                    </MudList>
                }
            }
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">@S["Cancel"]</MudButton>
        <MudButton Color="Color.Primary" Variant="Variant.Filled" OnClick="SubmitAsync" Disabled="@(_selectedResult == null || _isSubmitting)">
            @if (_isSubmitting)
            {
                <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
            }
            else
            {
                <MudText>@S["Confirm"]</MudText>
            }
        </MudButton>
    </DialogActions>
</MudDialog>
