@page "/settings/stats"
@inject NavigationManager NavigationManager
@inject IStringLocalizer<Settings> L

<PageTitle>@L["TabStats"]</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/my-space/stats", replace: true);
    }
}
