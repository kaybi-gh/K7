@page "/settings/video-player"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/settings/video-playback", replace: true);
    }
}
