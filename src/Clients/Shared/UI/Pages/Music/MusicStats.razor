@page "/music/stats"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/my-space/stats?type=MusicTrack", replace: true);
    }
}
