@page "/music/stats"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/my-space/stats?tab=music", replace: true);
    }
}
