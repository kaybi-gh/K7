@page "/admin/users"
@layout AdminLayout
@inject IStringLocalizer<AdminUsersPanel> L
@inject IStringLocalizer<SharedResource> S

<div class="d-flex align-center justify-between mb-4">
    <h6>@L["Title"]</h6>
    <K7Button Variant="filled" Color="primary" StartIcon="@Phosphor.UserPlus"
               OnClick="OpenCreateUserDialog" aria-label="@L["AriaCreateUser"]">
        @L["CreateUserTitle"]
    </K7Button>
</div>

@if (_isLoading)
{
    <K7ProgressBar Indeterminate="true" />
}
else if (_users.Count == 0)
{
    <div class="surface pa-8 d-flex flex-column align-center">
        <K7Icon Name="users" Size="lg" Color="secondary" />
        <h6 Color="secondary" class="mt-4">@L["Empty"]</h6>
    </div>
}
else
{
    <table class="k7-table k7-table--dense">
        <thead>
            <tr>
                <th>@L["HeaderUser"]</th>
                <th>@L["HeaderRole"]</th>
                <th>@L["HeaderActive"]</th>
                <th>@L["HeaderCreatedAt"]</th>
                <th style="width: 100px;">@L["HeaderActions"]</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in _users)
            {
                <tr style="@(!user.IsActive ? "opacity: 0.5;" : "")">
                    <td>
                        <div class="d-flex flex-col">
                            <p>@(user.UserName ?? "-")</p>
                            <span class="text-caption" Color="secondary">@(user.Email ?? "-")</span>
                        </div>
                    </td>
                    <td>
                        @if (user.IsGuest)
                        {
                            <K7Chip Size="sm" Color="default">@S["Guest"]</K7Chip>
                        }
                        else
                        {
                            <K7Select TValue="string" Value="@user.Role" ValueChanged="@(role => OnRoleChanged(user, role))"
                                       Variant="outlined">
                                <K7SelectItem Value="@Roles.Administrator">@L["RoleAdministrator"]</K7SelectItem>
                                <K7SelectItem Value="@Roles.User">@L["RoleUser"]</K7SelectItem>
                            </K7Select>
                        }
                    </td>
                    <td>
                        <K7Switch Value="@user.IsActive"
                                   ValueChanged="@(v => OnToggleActive(user, v))" />
                    </td>
                    <td>@user.Created.LocalDateTime.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>
                        <div class="d-flex">
                            @if (!user.IsGuest)
                            {
                                <K7IconButton Icon="@Phosphor.Sliders" OnClick="@(() => OpenCapabilitiesDialog(user))" AriaLabel="@L["AriaCapabilities"]" />
                            }
                            <K7IconButton Icon="@Phosphor.VideoCamera" OnClick="@(() => OpenLibraryExclusionsDialog(user))" AriaLabel="@L["AriaLibraries"]" />
                            <K7IconButton Icon="@Phosphor.EyeSlash" OnClick="@(() => OpenMediaExclusionsDialog(user))" AriaLabel="@L["AriaHiddenMedia"]" />
                            <K7IconButton Icon="@Phosphor.Shield" OnClick="@(() => OpenRestrictionProfileDialog(user))" AriaLabel="@L["AriaRestrictionProfile"]" />
                            @if (!user.IsGuest && user.Id != _currentUserId)
                            {
                                <K7IconButton Icon="@Phosphor.Key" OnClick="@(() => OpenResetPasswordDialog(user))" AriaLabel="@L["AriaResetPassword"]" />
                                <K7IconButton Icon="@Phosphor.GitMerge" OnClick="@(() => OpenMergeUserDialog(user))" AriaLabel="@L["AriaMerge"]" />
                                <K7IconButton Icon="@Phosphor.Trash" OnClick="@(() => ConfirmDelete(user))" AriaLabel="@L["AriaDelete"]" />
                            }
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
