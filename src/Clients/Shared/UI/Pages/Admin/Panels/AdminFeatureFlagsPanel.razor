@page "/admin/feature-flags"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/admin/libraries", replace: true);
    }
}
